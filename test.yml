services:
  db:
    image: mysql:latest
    container_name: db
    env_file:
      - .db
    expose:
      - "3306"
    ports:
      - "3306:3306"
    volumes:
      - ./schema.sql:/docker-entrypoint-initdb.d/init_a.sql
      - ./test.sql:/docker-entrypoint-initdb.d/init_b.sql
    healthcheck:
      test: ["CMD", "mysqladmin" ,"ping", "-h", "localhost", "-uroot", "-ppass"]
      interval: 5s
      timeout: 5s
      retries: 20